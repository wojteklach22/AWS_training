# How to start ?

### Configure aws config

Type in:

```
aws configure
```

Then enter your\
`AWS Acces Key ID`,\
`AWS Secret Access Key`,  
`Default region name`,\
`Default output format`\
After that your scripts should work fine